# Contracts

`openapi.yaml` e `schema.graphql` estão intencionalmente vazios. Os contratos conceituais estão documentados em `docs/3.0.0-RC/`. Eles serão materializados quando a arquitetura for aprovada e a implementação começar.
